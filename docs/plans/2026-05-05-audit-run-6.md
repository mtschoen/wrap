# Audit Run 6 — full audit of post-Phase-0 wrap skill

**Status:** ready to run, not started
**Created:** 2026-05-05
**Trigger:** This very document is the Phase 0 "Wrap with handoff" output of the session that introduced Phase 0. The audit was deferred to a fresh session per the user's *"hand off to a new session first."*

## What changed in this session

1. **New Phase 0 — Outstanding-asks check** added to `SKILL.md` (lines 48–58 post-renumber). Three-option fork: *Finish first* / *Wrap with handoff* / *Wrap, drop the rest*.
2. **All subsequent phases shifted by 1** — old Phase 0 → Phase 1, old 1 → 2 (with sub-phases 1a/1b → 2a/2b), old 2 → 3 (2a/b/c/d → 3a/b/c/d), old 3 → 4. Renumbered consistently in `SKILL.md`, `references/categories.md`, `references/hygiene-checklist.md`, `references/plan-classification.md`.
3. **Two new pressure scenarios added** — scenario 15 (clear unfinished business; THE Phase 0 fork test) and scenario 16 (borderline kvetch; anti-fabrication floor).
4. **Tool-name fix in scenario 13** — `KillShell` → `TaskStop`, `BashOutput` → `TaskOutput` (Run 4 had fixed the SKILL but not the scenario doc).
5. **AUDIT.md gained a "Phase numbering changed" note** between Run 3 and Known follow-ups, marking that runs above use pre-shift numbering.
6. **The new skill is installed** at `~/.claude/skills/wrap/` (verified line 48 = "Phase 0 — Outstanding-asks check"). Run 6 should target the installed copy.

## Deliverable

A new `## Run 6 — 2026-05-XX` section in `AUDIT.md` with rows for the 16 scenarios actually exercised, plus per-scenario evidence files in `docs/evidence/`.

## Scope priority

**Highest priority** (validate the new Phase 0 behavior):
- **Scenario 15** — clear unfinished business. Run all three branches if budget allows: *Finish first* (verify exit-clean), *Wrap with handoff* (verify externalization to a Phase 3a memory/plan), *Wrap, drop the rest* (verify Phase 4 summary lists the dropped items). At minimum run the *Finish first* branch — that's the highest-leverage path because it's the gate that proves Phase 0 can short-circuit wrap.
- **Scenario 16** — borderline kvetch. Verify Phase 0 does NOT surface the fork. Anti-fabrication floor parallel to scenario 12.

**Sanity-check coverage** (verify renumbering didn't break anything subtle):
- **Scenario 1** (clean repo) — Phase 0 must skip silently for genuinely-quiet sessions.
- **Scenario 2** (dirty + unpushed) — full Phase 0→4 happy path with the new numbering.
- **Scenario 4 or 6** — plan-classification path still works (Phase 3b).
- **Scenario 11** — `.claude/scripts/` hygiene (Phase 3c).
- **Scenario 13 or 14** — background-process / subagent loose-thread paths (Phase 2b → 2a feedback loop).

**Lower priority** (cover only if budget allows): 3, 5, 7, 8, 9, 10, 12.

## Runner approach (per `AUDIT.md` follow-up #2)

- **Option B — `--permission-mode bypassPermissions`** for safe / read-mostly: scenarios 1, 2, 9, 11, 12, **16**.
- **Option A — `--input-format stream-json` with scripted responses** for fork-resolution: scenarios 3, 4, 5, 7, 8, **15** (each branch is a separate response sequence).
- **`--output-format stream-json`** for tool-trace verification: scenarios 13, 14, **15**, **16** (need to prove Phase 0 ordering).

Mix per-scenario as appropriate. Capture transcripts under `docs/evidence/NN-<slug>.md` matching the existing convention. Scenario 15's three branches → three evidence files (e.g. `15a-finish-first.md`, `15b-wrap-handoff.md`, `15c-drop-rest.md`).

## Key things to verify in Phase 0 behavior

- **Scenario 15 fork prompt** offers exactly the three options (`Finish first` / `Wrap with handoff` / `Wrap, drop the rest`). Two-option drift is a fail.
- **Scenario 15 branch outcomes:**
  - *Finish first*: wrap exits with no scope detect, no commits, no memory writes. The agent returns control with the unfinished tasks visible in conversation.
  - *Wrap with handoff*: tasks land in a Phase 3a memory entry or plan file with concrete content (not just "TODO"). Phase 4 summary names the destination.
  - *Wrap, drop the rest*: tasks NOT externalized but DO appear in Phase 4 summary's leftovers/rejected list.
- **Scenario 16:** No fork prompt. Wrap proceeds straight from Phase 0 to Phase 1. No fabricated "outstanding asks."
- **Ordering invariant:** Phase 0's `AskUserQuestion` (when it fires) must precede ANY Phase 1 tool call (`git status`, `Bash` against repo state, scope-confirmation question with the repo list). Stream-json grep for the first `tool_use` event after wrap launches should be `AskUserQuestion`, not git/Bash.

## Reference

- New `SKILL.md`: `~/skills-dev/wrap/SKILL.md`
- All scenarios: `~/skills-dev/wrap/docs/pressure-scenarios.md`
- Run 4 / Run 5 wrote evidence in stream-json mode — reuse their patterns.
- AUDIT.md follow-up #2 has Option A/B detail.
- This session's wrap commit (`Wrap-Session-Id` trailer) is the audit-trigger context. Cite the SHA in Run 6's intro.

## On completion

- Add `## Run 6 — 2026-05-XX` section to `AUDIT.md` with the scenario table and any narrative findings.
- If any scenario fails, treat it as a real skill gap and follow Run 4's pattern: capture evidence, fix the skill in source, re-install, re-run the failing scenario. Don't paper over a fail.
- Per `references/plan-classification.md` "Completed + tracked → delete outright" rule, **delete this plan file** once Run 6's evidence is in AUDIT.md. Git history preserves it.
